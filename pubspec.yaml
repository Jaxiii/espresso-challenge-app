name: espresso_challange
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  flutter_bloc: ^8.1.5
  equatable: ^2.0.5
  api:
    path: ../api
    # git:
    #   url: https://github.com/Jaxiii/espresso-challenge-api.git
    #   ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^2.4.0

flutter_native_splash:
  color: "#B4A270"
  image: assets/images/logo.png
  web: false
  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/logo.png

